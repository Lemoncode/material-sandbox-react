import { css } from 'emotion';

export const root = css`
  && {
    margin: 2rem;
    flex-shrink: 0;
    padding: 0;
    border: 1px solid darkgray;
    border-radius: 0;
  }
`;

export const item = css`
  font-size: 2rem;
`;
