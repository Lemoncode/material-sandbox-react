import { css } from 'emotion';

export const root = css`
  && {
    margin: 2rem;
    flex-shrink: 0;
    border-radius: 0;
    box-shadow: 0 0 10px black;
  }
`;

export const label = css`
  font-size: 2rem;
`;
